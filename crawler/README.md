# 植物数据爬虫项目

## 📖 项目说明

本项目用于爬取百度百科的植物数据，建立本地中文植物数据库。

## 🚀 快速开始

### 1. 安装依赖

**Python 依赖：**
```bash
pip install requests beautifulsoup4
```

**Node.js 依赖：**
```bash
cd crawler
npm install mysql2
```

### 2. 初始化数据库

```bash
# 使用 MySQL 客户端执行
mysql -u root -p < init_database.sql
```

或者在 MySQL Workbench 中执行 `init_database.sql`

### 3. 运行爬虫

```bash
python baike_crawler.py
```

爬虫会：
- 爬取 100+ 种常见植物数据
- 自动保存到 `plants_data.json`
- 每个请求间隔 2 秒（避免被封）

### 4. 导入数据

```bash
node import_data.js
```

可选参数：
- `--clear`: 清空现有数据后导入

```bash
node import_data.js --clear
```

## 📁 文件说明

```
crawler/
├── baike_crawler.py      # 百度百科爬虫脚本
├── init_database.sql     # 数据库初始化脚本
├── import_data.js        # 数据导入脚本
├── plants_data.json      # 爬取的数据（自动生成）
├── package.json          # Node.js 依赖配置
└── README.md             # 本文件
```

## 🌿 爬取的数据字段

- `chinese_name`: 中文名
- `scientific_name`: 学名
- `english_name`: 英文名
- `alias`: 别名
- `family`: 科
- `genus`: 属
- `description`: 简介
- `morphology`: 形态特征
- `habitat`: 生长环境
- `distribution`: 分布地区
- `flowering_period`: 花期
- `image_url`: 图片链接

## ⚙️ 配置

### 数据库配置

在 `import_data.js` 中修改：

```javascript
const dbConfig = {
    host: 'localhost',
    user: 'root',
    password: 'your_password',
    database: 'plant_assistant'
};
```

或使用环境变量：
```bash
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password
DB_NAME=plant_assistant
```

### 爬取植物列表

在 `baike_crawler.py` 中修改 `COMMON_PLANTS` 列表：

```python
COMMON_PLANTS = [
    '玫瑰', '牡丹', '菊花',
    # 添加更多植物...
]
```

## 📊 使用统计

- 默认爬取：100+ 种常见植物
- 爬取速度：约 30 条/分钟（含延时）
- 数据大小：约 50-100KB/条

## ⚠️ 注意事项

1. **遵守 robots.txt**：爬虫已设置合理延时
2. **数据版权**：仅用于学习和个人项目
3. **网络稳定**：建议在网络稳定时运行
4. **错误处理**：部分植物可能爬取失败（正常现象）

## 🔧 故障排除

### 爬虫失败
- 检查网络连接
- 增加延时时间
- 检查百度百科是否可访问

### 数据库连接失败
- 检查 MySQL 是否运行
- 验证数据库配置
- 确认数据库已创建

### 导入失败
- 检查 `plants_data.json` 是否存在
- 验证 JSON 格式是否正确
- 查看错误日志

## 📝 后续步骤

1. 运行爬虫获取数据
2. 导入数据到数据库
3. 修改后端接口使用本地数据库
4. 测试搜索功能

## 🎯 扩展功能

- [ ] 添加更多数据源
- [ ] 图片下载到本地
- [ ] 数据去重和清洗
- [ ] 定期更新数据
- [ ] 添加数据统计功能
